# OS files
.DS_Store
Thumbs.db
desktop.ini

# Superpowers brainstorm sessions
.superpowers/

# Editor files
*.swp
*.swo
*~
.vscode/

# Claude Code local settings (personal, not shared)
.claude/settings.local.json

# Node modules (if any npm packages installed locally)
node_modules/

# Logs
*.log

# Backup files
*.bak
*.backup

# Research documents (source material, not deployed)
*.docx
*.pdf
*.xlsx

# Local folders (not deployed)
photos/
internal/
dist/

# Build artifacts
dist/

# Temp build files
testing/preserved_data.txt
testing/v0_*.jsx
testing/v0_*.css
testing/v0_*.js

# v0 export (Next.js source, not deployed)
testing/b_NUquevGyH7/
testing/b_NUquevGyH7U.zip

# Old babel backup
index-old-babel.html

# Environment secrets
.env
.env.local
.env.*.local

# Bundle report
bundle-report.html
dist/bundle-report.html

# Temp image extraction
temp_images/

# Claude Code worktrees
.claude/worktrees/

# Setup prompts (not deployed)
master-setup-prompt.md
site-audit-for-creative-director.md

# Playwright MCP cache
.playwright-mcp/

# Peru 2026 raw trip media (Miami→Lima→Salkantay→Machu Picchu).
# ~30 videos + 200+ photos. Raw media NEVER enters git history — it is
# large, binary, and unrecoverable from a repo without bloating every
# future clone. Generated review artefacts live under Peru26/review/.
Peru26/

# Visual QA screenshots (generated)
audit/
/jordi-*.png

# rollup-plugin-visualizer output (local only, never deployed)
.bundle-report.html

# Playwright verification screenshots. The MCP browser writes these to the
# repo ROOT, not into .playwright-mcp/, so a bare `git add -A` sweeps 2.7MB of
# debug PNGs into the repo — caught by /ship on Sept 2 2026, same class of
# mistake as the bundle-report.html that shipped in August.
/fi-*.png
/local-list-*.png
/pulse-*.png
/tw-*.png
/views-map.png
/map*.png
/ship*.png
/final-*.png
